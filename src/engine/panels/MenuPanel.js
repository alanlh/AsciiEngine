class MenuPanel extends PanelBase {
  constructor(name, type, manager, settings) {
    super(name, type, manager, settings);
    
    this.settings = UtilityMethods.initializeArgs({
      // TODO:
    }, settings);
  }
  
  // Simply a holder for DataElements
}
